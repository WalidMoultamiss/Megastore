import type { Resolvers } from "@generated/types";
import { ICategory, Category, Product, Order, Cart
} from "@models/index";

export const resolvers: Resolvers = {

  
  Query: {
    // // @ts-ignore
    // getAllCarts: (): any => {
    //   return Cart.find();
    // },
  },
  Mutation: {
    
    }
};
