
export enum Role {
    USER = 'USER',
    SELLER = 'SELLER',
  }

  export enum StoreStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
  }

export enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    DELETED = 'DELETED',
    PENDING = 'PENDING',
}